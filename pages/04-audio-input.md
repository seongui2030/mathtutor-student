
음성 입력은 프로젝트의 첫 단계입니다.

사용자가 마이크를 통해 질문을 말하면 `record_audio_to_wav()`가 호출됩니다. 이 함수는 `sounddevice` 라이브러리를 통해 음성을 녹음하고, 지정 경로의 WAV 파일로 저장합니다.

이 단계에서 구현되는 핵심 내용은 다음과 같습니다.

- 마이크 권한 확인
- 녹음 시간 설정
- 샘플레이트 고정
- WAV 파일 생성

실제 생성 파일명은 `speech_transcribe.wav`입니다.
