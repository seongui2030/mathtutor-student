##09-2. MP3 파일 생성

TTS 단계가 끝나면 생성된 음성 파일이 저장됩니다.

```python
speech_file_path = Path(__file__).parent / "speech_tts.mp3"
```

이 파일은 다시 `play_mp3()`로 넘겨져 실제 스피커로 재생됩니다. 이렇게 최종적으로 “듣기 가능한 응답”이 사용자에게 전달됩니다.

이 과정을 통해 프로젝트는 문장 생성 → 음성 변환 → 재생이라는 사용자 친화적 루프를 완성합니다.
