# 07. 수학 용어 가드레일

정규화된 문장은 `input_guardrail()` 함수에서 수학 질문인지 판별합니다.

이 단계는 단순히 문장이 명령형인지 판단하는 것이 아니라, 수학 문제로 풀이 가능한 입력인지 검증합니다.

핵심 아이디어:

- `math_json/math.json` 파일에 정의된 용어와 표현을 사용한다.
- 입력 문장에 해당 표현이 포함되면 수학 처리 경로로 분기한다.
- 그렇지 않으면 일반 GPT 응답 경로로 넘어간다.

즉, 수학이라는 도메인에서만 특정 동작을 하도록 설계된 방어 장치입니다.
