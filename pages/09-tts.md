# 09. TTS 변환

응답이 준비되면, 마지막 단계는 TTS로 바꾸는 것입니다.

`voice.py`의 `call_tts()` 함수는 OpenAI의 음성 합성 API를 호출해 문자열을 오디오 파일로 변환합니다. 결과물은 보통 `speech_tts.mp3`라는 이름으로 저장됩니다.

이때 `voice="alloy"`와 같은 옵션을 통해 목소리 톤을 설정할 수 있으며, 사용자에게 더 친근한 음성을 제공합니다.
