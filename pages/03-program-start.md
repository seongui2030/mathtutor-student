# 03. 프로그램 실행

프로젝트의 실행은 `total_mic_speak_mcp.py`에서 시작됩니다.

이 파일은 아래의 역할을 수행합니다.

- dotenv 로딩
- API 키 준비
- MCP 서버 설정
- 음성 녹음
- STT 처리
- 도메인 정규화
- 수학 용어 비교
- GPT 응답 생성
- TTS 음성 생성
- MP3 재생

프로그램이 실행되면 `__main__` 블록이 동작하고, `main()` 함수가 호출됩니다.
